const fs = require("fs"); const path = require("path"); const root = path.join(process.cwd(), "node_modules", "better-auth", "dist"); const patterns = [/createAuthClient/, /toNextJsHandler/, /betterAuth\(/, /socialProviders/, /signIn\\.social/, /authClient/]; function walk(dir){ for(const entry of fs.readdirSync(dir,{withFileTypes:true})){ const full = path.join(dir, entry.name); if(entry.isDirectory()) walk(full); else if(entry.isFile() && full.endsWith(".mjs")){ const content = fs.readFileSync(full, "utf8"); for(const pat of patterns){ if(pat.test(content)){ console.log("MATCH", full, pat); content.split("\n").forEach((line, idx) => { if(pat.test(line)) console.log(idx+1, line.trim()); }); return; } } } } } walk(root);
